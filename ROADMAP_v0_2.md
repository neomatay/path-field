---
knowledge_type: roadmap
lifecycle: frozen
scope: "PATH v0.2 Field Alpha：本地优先 PWA 的范围冻结与里程碑"
verified_at: "2026-08-17"
sources:
  - "V1_PRODUCT_CONTRACT.md"
  - "05-decisions/ADR-001-launch-wedge-and-mvp-scope.md"
  - "05-decisions/ADR-002-defer-formal-user-testing.md"
  - "DESIGN.md"
---

# PATH v0.2 Field Alpha：范围冻结与里程碑

**形态：** 移动优先的 Web PWA（可安装到手机主屏），本地优先，无后端。
**验证方式：** 创始人自用 2-4 周（见 [ADR-002](05-decisions/ADR-002-defer-formal-user-testing.md)）。
**本文作用：** v0.2 开发期间的范围事实源。与早期文档（PRODUCT_FRAMEWORK、V1_PRODUCT_CONTRACT 等）冲突时，以本文为准。

## 1. 版本路线

| 版本 | 目标 | 状态 |
|---|---|---|
| v0.1 | 可点击、虚构数据的概念原型 | 已完成 |
| **v0.2 Field Alpha** | 使用真实个人数据的本地优先 PWA | **当前阶段** |
| v0.3 | 账户、云同步、导入导出服务端、3-5 人轻量测试、小范围多人试用 | v0.2 自用通过后 |
| v1.0 | 原生能力、HealthKit/穿戴、通知、商业化探索 | 暂不开始 |

## 2. v0.2 能力清单（做）

1. **首次状态建立**：训练阶段自选（重返 / 建立规律 / 初步进阶）、目标、可用时间、场地、限制与安全分流。
2. **当前 Mission**：4-6 周方向、成功证据、非唯一裁判指标、复盘日期；创建与修改均由用户确认。
3. **今日训练**：完整 / 短版 / 恢复版，依据可用时间与状态给出建议版本及理由；任何版本可选。
4. **真实训练记录**：动作、组数、重量、次数、RPE、替换、跳过、不适标记；记录以完成当下所需的最少字段为限。
5. **计划调整**：不自动加重量；给出候选调整（含依据与未知项）并由用户确认。
6. **周复盘**：事实 / 观察 / 待确认 / 候选路线，用户确认下一步。
7. **中断重返**：中断 >= 14 天触发重新确认；不清零、不显示欠债、不自动恢复旧负荷。
8. **本地数据**：IndexedDB 存储、一键 JSON 导出/导入、添加到主屏幕引导。
9. **路线推进最小可视化**：当前 Mission 与已完成训练以一条可推进的路线表达（克制版，非完整海报）。
10. **单次训练收据**：训练完成后的事实简报（事实在前，观察与未知在后），即 v0.1 原型的"野外记录"形式。

**三条状态路径都保留**（重返 / 建立规律 / 初步进阶各有模板），与 ADR-001 的首发收窄不冲突：v0.2 的**验证**以创始人自用（初步进阶型）为主场景，其余两条路径为可走通但未验证状态。

## 3. v0.2 明确不做

- 通用聊天式 AI 教练、动作识别、伤病诊断、相机纠错。
- 精确饮食称重、卡路里打卡、体重目标管理（体重仅作为可选证据记录）。
- 课程商城、私教交易、社交动态、排行榜。
- Apple Watch / HealthKit 同步、推送通知、支付。
- 账户体系、云同步、后端（全部推迟到 v0.3）。
- 完整国家公园阶段海报（v0.2 只做收据 + 路线推进；完整海报推迟到 v0.3）。
- 周期化编程、专项运动计划、多用户协作。

## 4. 里程碑（垂直切面，每周有可用产物）

| 里程碑 | 时间 | 交付 | 你能做什么 |
|---|---|---|---|
| **M0 范围冻结** | 第 1-2 天 | 本文档 + ADR-002 + 三套模板动作清单（用创始人真实动作填写） | 确认范围与训练内容 |
| **M1 骨架跑通** | 第 1 周 | PWA 脚手架、core 规则包（含单测）、IndexedDB 存储层、JSON 导出 | 装到手机主屏，跑通空状态 |
| **M2 第一次真实使用** | 第 2 周 | 今天页（状态检查 + 三版本选择）、训练执行与记录、单次收据 | **在健身房用真实训练完整记录一次；从这天起可弃用训记** |
| **M3 闭环补全** | 第 3-4 周 | 周复盘、Mission 设定与修改、计划调整确认、中断重返流、路线推进可视化 | 一周训练全流程在产品内闭环 |
| **M4 自用验证期** | 第 5-6 周起 | 修自用发现的问题，记录 `SELF_USE_FINDINGS.md` | 连续 2-4 周真实使用，按 ADR-002 标准判定 |

时间按工作日晚间 + 周末投入估算；周末整块投入可压缩。

## 5. 训练内容最小集（M0 交付）

只内置三套可配置模板，动作取自创始人真实训练：

| 模板 | 服务路径 | 动作来源示例 |
|---|---|---|
| 重返版全身训练 | 重返 | 低门槛基础动作 + 恢复活动 |
| 全身 A / B | 建立规律 | 基础动作，固定时段，每次 20 分钟短版可用 |
| 下肢重点计划 | 初步进阶 | 腿举、髋外展/内收、深蹲、臀推等（创始人当前真实臀腿日动作） |

每个动作配 **2-3 个替换动作**（覆盖器械被占用场景），构成最小动作替换表。动作实体进数据模型（见第 6 节），不硬编码在页面里。

## 6. 数据模型（相对 V1_PRODUCT_CONTRACT 的增改）

`CurrentState / Mission / Program / Session / Evidence / Review / Decision` 七对象沿用契约定义，v0.2 做以下调整：

1. **新增 `Exercise` 实体**：`id`、名称、器械依赖、`substitutionGroupId`（替换组）。`Session.actualBlocks` 引用 `Exercise.id`。
2. **简化 `comparisonKey`**：v0.2 可比条件 = 同模板版本 + 同动作 + RPE 区间相近；契约中的"口径版本"字段保留但暂不实现校验。
3. **`source` 收窄**：v0.2 只有 `user` 与 `derived`，无 `device` / `import`。
4. **规则集实现范围**：SAFE-URGENT-01 / SAFE-CAUTION-02 / SAFE-CLEAR-03、VARIANT-FULL/SHORT/RECOVERY/USER、RETURN-14D/FIRST/NEXT、REVIEW-WEEK-LOW/SIGNAL、REVIEW-BOUNDARY-04。REVIEW-MISSION-03 的完整阶段复盘推迟到 v0.3，v0.2 只做 Mission 到期提醒与手动复盘。
5. **进阶规则保持最简**：候选进阶仅"同 RPE 下完成目标区间上限 -> 候选小幅加重（如 +2.5kg），需用户确认"；不做周期化。

## 7. 技术决策

- **前端**：React + TypeScript + Vite，PWA（Web App Manifest + Service Worker，离线可用）。
- **数据**：IndexedDB（本地优先），一键导出/导入 JSON。
- **架构分层**：`core/`（领域模型 + 规则，纯 TS、无 UI 依赖、可单测）先行；UI 只消费 core 的输入输出。规则实现必须可追溯到契约中的 `ruleId`。
- **后端**：无。v0.3 再评估 Spring Boot + PostgreSQL。
- **数据安全**：首次使用即引导"添加到主屏幕"（iOS 会清理未安装 PWA 的站点数据）；每次训练结束提示可导出；导出文件默认含全部本地数据。

## 8. 验收（M4 出口）

- 三条路径在真实数据下均可走通（重返路径允许模拟中断触发）；
- 全部已实现规则有单测，且每条建议在 UI 上可回溯到 ruleId 与输入证据；
- urgent 安全信号在 UI 上抑制完整训练入口（契约第 6.4 条）；
- 本地数据可完整导出并恢复；
- 达到 ADR-002 自用通过标准的全部 5 条。

## 9. 文档状态同步

| 文档 | 状态调整 |
|---|---|
| [V1_PROTOTYPE_VALIDATION.md](V1_PROTOTYPE_VALIDATION.md) | `deferred`：推迟到 v0.3 前复用 |
| [USER_TEST_EXECUTION_PACK_V0_1.md](04-task-playbooks/USER_TEST_EXECUTION_PACK_V0_1.md) | `deferred`：同上 |
| [ADR-001](05-decisions/ADR-001-launch-wedge-and-mvp-scope.md) | 人群收窄**仅指 v0.2 验证范围**——产品定位始终是覆盖全旅程所有阶段、用户自选阶段（见 ADR-001 决定首段，"不是永久市场排他"）；P0 边界有效；验证闸门由 ADR-002 取代 |
| `SELF_USE_FINDINGS.md` | 随 M2 创建，自用发现统一记录处 |

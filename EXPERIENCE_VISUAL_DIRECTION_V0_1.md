# 健身成长系统：体验与视觉方向 v0.1

**状态：** 框架阶段设计方向，供信息架构、低保真原型和设计系统使用。  
**目的：** 定义用户感受、视觉语言和交互约束；不定义最终品牌名、Logo 或完整页面细节。

## 1. 方向结论

产品采用原创的 **“安静成长导航”** 方向：以清晰、克制的数字产品秩序承载训练判断，以自然材料和地形般的空间感呈现成长，以低刺激、非竞争的节奏陪伴长期使用。

它从用户偏好中提取的是方法，而不是复刻现有作品：

- 简约数字产品的秩序、精确、留白和可信度；
- 东方材质感所带来的深度、庄重和身体性；
- 开放探索中“看得见方向、自己选择路径、能力逐步扩展”的体验。

产品不应成为硬核健身房工具、医疗仪表盘、可爱陪伴游戏或打卡竞赛平台。

## 2. 体验情绪

| 时刻 | 用户应感受到 | 设计应做到 |
|---|---|---|
| 打开产品 | 我知道当前处于什么状态 | 首页只有状态、Mission 和下一步 |
| 开始训练 | 我知道为什么练这个 | 每个训练展示目标、上次证据与可选版本 |
| 训练结束 | 这次留下了有意义的东西 | 先显示事实，再提示下一次需要观察什么 |
| 查看复盘 | 我能理解变化，不被单一数字审判 | 区分证据、推断、缺失信息与建议 |
| 中断后回来 | 我仍可从这里继续 | 不显示失败天数，先提供重返位置与可完成行动 |

体验关键词：**笃定、舒展、诚实、温和、有分量。**

## 3. 设计原则

1. **下一步优先。** 每个页面首先回答用户当前能做什么，统计信息退居第二层。
2. **信息有证据层级。** 事实、推断、建议和待确认事项使用不同的排版与颜色语义。
3. **质感来自材质和比例。** 使用纸、石、墨、植被般的低对比纹理与留白，不使用金属、火焰、霓虹或重型游戏装饰。
4. **成长是地形，不是等级。** Mission、能力与阶段变化构成路径；不使用金币、战斗力、抽卡和连续签到。
5. **身体不是成绩单。** 体重、围度、照片等内容默认克制且可选，永远不制造失败或羞耻状态。
6. **训练现场必须清楚。** 记录、替代、暂停和风险入口始终可见；训练页优先保证可读性与稳定布局。

## 4. 视觉语言

### 4.1 色彩

色彩用于表达状态，而不是营造单一主题。默认以明亮中性底色呈现，训练过程中允许切换高对比深色模式。

| 语义 | 建议颜色 | 用途 |
|---|---|---|
| Paper | `#F7F8F5` | 页面背景和大面积留白 |
| Ink | `#18231F` | 主要文字、结构和训练模式底色 |
| Jade | `#2A765F` | 稳定进展、可执行下一步、已确认状态 |
| Cobalt | `#5774C6` | 计划、信息、待确认建议 |
| Coral | `#D75C4A` | 疼痛、风险、停止或需要关注的信号 |
| Reed | `#D6BD71` | 阶段节点、低频强调，不用于警告 |
| Mist | `#E5E9E2` | 分隔、容器背景和未完成状态 |

不得只靠颜色表达状态；风险、完成、建议和缺失信息均需要文字和图形区分。

### 4.2 字体与数字

- 中文使用系统无衬线字体优先，iOS 为 PingFang SC，Web 为系统字体栈；不引入装饰字体干扰训练现场阅读。
- 数字使用等宽或近似等宽的数字特征，方便比较重量、次数、时间与趋势。
- 页面标题紧凑但不夸张；训练数值与动作名称优先于大号营销式文案。
- 不以斜体、花体或拟古文字表现东方感，质感应来自比例、材质和空间。

### 4.3 材质与图形

- 使用轻微纸张颗粒、地图等高线、路径刻度和低饱和色块，制造“训练图谱”的空间感。
- 肌群图、趋势线和能力地图必须服务于判断，不能成为装饰性仪表盘。
- 插画仅在空状态、阶段节点或教育内容出现；人物应多元、真实、非性化。
- 不使用复刻其他游戏的角色、武器、场景、字体、标志性构图或素材。

### 4.4 动效与声音

- 页面切换和状态更新使用短、平缓的过渡；记录完成以轻量确认替代庆祝爆炸。
- 训练开始、完成、风险提示的反馈有明确层级；风险提示不使用惊吓式动效。
- 声音默认关闭，不以提示音制造训练压力。

## 5. 核心页面视觉规则

### 今天

页面由“当前状态 -> 当前 Mission -> 下一步”组成。只保留一个主操作，展示完整、短版或恢复版的选择依据；训练数据不占据首页首屏。

### 训练

使用高对比、低干扰布局。动作名称、目标区间、上次表现、记录输入、替代和不适入口固定位置，避免训练中页面跳动。深色训练模式采用墨绿色而非纯黑，减少健身房强光下的刺眼感。

### 旅程

以能力地形和 Mission 路径呈现成长。阶段节点显示“已验证的变化”和“下一段需要积累的证据”，不展示空泛等级或连胜。

### 复盘

遵循固定阅读顺序：**发生了什么 -> 可以怎样理解 -> 还缺什么 -> 可选的下一步。** 体重和热量仅作为证据之一，不能压过表现、恢复和身体观察。

### 重返

页面视觉应最简、最少历史负担。保留用户的训练足迹，但只强调“你从哪里回来”和“今天能做什么”。

## 6. 不做的风格

- 黑金金属、机械纹理、猛兽图腾、火焰/闪电等硬核健身房套路；
- 高饱和霓虹、赛博 UI、过多发光边框和玻璃拟态；
- 婴幼儿化角色、撒娇文案、无意义经验值和每日任务轰炸；
- 医疗蓝白、风险色滥用和密集 KPI 看板；
- 用前后对比身材图、极端数字或体重趋势制造焦虑。

## 7. 关键组件规则

| 组件 | 规则 |
|---|---|
| Mission 卡 | 显示当前优先方向、成功证据、复盘日期和可修改入口 |
| 下一步卡 | 只显示一个明确动作；时间不足时提供短版，不以“跳过”羞辱用户 |
| 证据卡 | 标注数据来源与可信度；事实和推断不混在同一文案中 |
| 训练组记录 | 布局尺寸固定，记录前后不移动；支持快速复用上次表现 |
| 建议 | 必须附“为什么”；提供接受、修改、暂不采用三种真实选择 |
| 风险状态 | 使用珊瑚色、文字说明和明确停止/求助路径，不以得分或勋章包装 |
| 成长节点 | 使用路径、地形和阶段标记表达；需要对应真实训练或身体证据 |

## 8. 验证方法

在高保真设计前，为以下四个页面制作同一数据条件下的中保真样稿：今天、训练、复盘、重返。

测试重点：

1. 用户是否能在 5 秒内找到下一步和其理由；
2. 用户是否将视觉风格感知为专业、舒服、非评判，而不是医疗或游戏化；
3. 用户是否能区分事实、推断与建议；
4. 数据密度在训练现场和复盘场景是否分别合适；
5. 身体数据是否让用户更理解自己，而非更焦虑。

## 9. 本阶段交付物

1. 本文档确认后的 v1.0；
2. 原创方向板：色彩、字体、材质、数据表达、训练与复盘样例；
3. 四个关键页面的中保真探索；
4. 颜色语义、间距、文字层级、状态和组件的设计令牌草案；
5. 与《产品框架定义 v0.1》一致的页面和文案审查清单。

在上述内容经用户测试确认前，不制作大规模插画、完整课程视觉、复杂动效或品牌营销页。

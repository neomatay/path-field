# 健身成长系统：产品框架定义 v0.1

**状态：** 讨论稿，供原型、用户研究与技术架构共同使用  
**版本目标：** 固定产品的核心闭环与扩展边界，不定义具体训练处方或视觉风格。

## 1. 产品定义

### 1.1 产品承诺

帮助用户从自己的当前训练状态出发，理解身体与训练的证据，完成安全、可执行的下一步，并在生活变化或中断后重新找到方向。

产品不是课程货架、热量记账工具、运动数据仪表盘，也不替代医疗、康复或专业教练。它是一个把目标、现实约束、训练和反馈连接起来的**自适应训练成长系统**。

### 1.2 要解决的问题

用户常拥有训练记录、体重、穿戴数据甚至上过私教课，却仍无法可靠回答：

- 我当前最该追求什么，而不是同时追逐彼此难以比较的目标？
- 今天适合做什么；时间、状态或器械不满足时，如何安全调整？
- 我是否真的在进步；体重、外观、力量和感受不一致时该如何理解？
- 计划为什么要改变；我能否接受、修改或拒绝它？
- 停练后如何回来，而不是把中断视为失败？

### 1.3 不变原则

1. **自主优先：** 用户选择目标、路径、数据权限和最终方案；系统给出有依据的建议。
2. **证据诚实：** 显示事实、推断、缺失信息及其置信度，绝不把估计包装为结论。
3. **安全优先：** 训练建议服从风险与疼痛护栏；高风险信号不被游戏化或激励化。
4. **以成长而非服从为奖励：** 不用体重、连续打卡、热量赤字或羞耻作为主要驱动。
5. **为中断设计：** 忙碌、假期和状态下滑是正常状态，计划必须可降级、暂停与重启。

## 2. 用户成长状态模型

用户不是固定画像，而是可进入、升级、暂停和回退的训练状态。系统初始以用户自选为主，并根据用户确认的训练证据提出调整建议。

| 状态 | 用户的核心问题 | 系统首要交付 | 进入/离开信号 |
|---|---|---|---|
| 未开始或重返 | 如何安全开始，而不被复杂度吓退 | 最小行动、短版训练、重返校准 | 近期无训练或主动选择重新开始 |
| 建立规律 | 如何把训练放进现实生活 | 基础动作/模板、重复节奏、低摩擦记录 | 可稳定完成简单训练周期 |
| 有计划训练 | 如何让计划适应时间、器械与恢复 | 周计划、替代方案、训练执行反馈 | 用户开始比较训练表现与目标 |
| 自主进阶 | 如何判断有效、何时调整 | 阶段目标、进展解释、可协商调整 | 用户可围绕目标进行计划性训练 |
| 深度探索 | 如何围绕专项目标持续实验 | 周期化、专项能力、专业协作入口 | 用户需要更高专业度或更细目标 |

状态不是等级，不会锁住内容。它只控制默认信息密度、推荐深度和必要安全检查。用户可随时改变自我描述。

## 3. 核心闭环

```text
当前状态与限制
  -> 当前阶段目标（Mission）
  -> 可执行计划
  -> 实际训练与即时调整
  -> 训练/身体/生活证据
  -> 周复盘与阶段解释
  -> 用户确认下一步
  -> 更新状态、Mission 与计划
```

### 3.1 当前状态

`CurrentState` 只收集当前决策必要信息：可用时间、器械环境、训练经验、近期活动、主观精力、酸痛/不适、已知限制和可选健康平台信号。

首次进入必须先做简短安全分流。出现胸痛、晕厥、异常呼吸困难、急性/持续恶化疼痛等风险信号时，系统停止生成高强度训练，建议寻求合适专业评估。

### 3.2 阶段目标（Mission）

`Mission` 是 4-6 周内唯一优先的训练方向，不等于用户的长期愿望。它由以下内容组成：

- 用户要改变的能力或体验；
- 用户可投入的时间、环境和限制；
- 本阶段的成功证据；
- 本阶段不作为唯一裁判的指标；
- 何时复盘、何时调整。

示例：用户长期希望获得更匀称的体型，但当前 Mission 可以是“稳定完成下肢训练并建立可比较的表现证据”。体重可被观察，但不自动成为阶段成败的唯一依据。

### 3.3 训练计划

`Program` 是一个阶段的训练路线，包含：

- 每周的推荐节奏与可选训练日；
- 每次训练的目标、动作组、目标区间和训练理由；
- 时间不足时的短版；
- 恢复不足或不适时的恢复版/替代方案；
- 明确的进阶、维持、降载或复盘条件。

计划是可协商的假设，不是强制命令。用户可替换、跳过或暂停；系统记录原因，以便后续提高计划匹配度。

### 3.4 训练证据

`Evidence` 统一容纳不同来源、不同可信度的信号：

| 证据类型 | 示例 | 产品处理方式 |
|---|---|---|
| 训练表现 | 可比工作组、次数、主观难度、动作稳定性 | 呈现趋势，不用单次数据判定进步 |
| 身体观察 | 围度、周期照片、衣物松紧、用户自述线条变化 | 自愿、阶段性记录，不鼓励每日审视 |
| 主观体验 | 疼痛/不适、精力、信心、疲劳、训练意愿 | 标注为主观信号，不冒充医学结论 |
| 生活上下文 | 睡眠、步数、饮食模式、压力、训练时间 | 仅在影响当前判断时调用，支持跳过 |
| 设备数据 | Apple Health、Health Connect、心率等 | 显示来源和采集限制，不自动等同训练质量 |

### 3.5 复盘与调整

`Review` 每周轻量、每个 Mission 结束时完整进行。它必须依次回答：

1. 本阶段实际发生了什么？
2. 哪些信号支持或反驳当前判断？
3. 哪些关键数据缺失，因而不能下结论？
4. 为什么建议维持、进阶、恢复或转向？
5. 用户选择哪条路线，原因是什么？

## 4. 功能架构

### 4.1 必要模块

| 模块 | 负责内容 |
|---|---|
| 起始与安全 | 安全分流、状态选择、限制记录、数据授权 |
| 目标与 Mission | 长期愿望、当前阶段优先级、成功证据与复盘日期 |
| 计划引擎 | 模板选择、计划生成、短版/恢复版、可解释调整 |
| 训练执行 | 训练卡、上次表现、记录、替换、主观难度与不适反馈 |
| 证据中心 | 表现、观察、感受和设备数据的可比趋势 |
| 复盘中心 | 周总结、阶段总结、候选路线与用户确认 |
| 成长地图 | 当前 Mission、能力变化、阶段里程碑和重返位置 |
| 重返机制 | 中断校准、低压力恢复计划、历史上下文保留 |
| 隐私与连接 | 数据来源、使用目的、删除/导出、健康平台连接 |

### 4.2 后续模块（预留边界，不进入 v1）

- 细粒度营养记录和食物库；
- 计算机视觉动作评估；
- 实时 AI 对话教练；
- 教练协作、社群、挑战和商业化；
- 可穿戴设备深度联动和多市场内容运营。

上述能力只能通过既定的 `Evidence`、`Decision`、`Program` 和权限接口接入，不能绕过安全与解释层直接影响训练计划。

## 5. 体验信息架构

### 5.1 移动端：训练现场

| 一级入口 | 用户任务 |
|---|---|
| 今天 | 查看当前状态、选择下一步、开始完整/短版/恢复训练 |
| 训练 | 执行动作、记录实际情况、标记替换和不适 |
| 旅程 | 了解当前 Mission、能力地图、阶段里程碑和重返位置 |
| 复盘 | 查看证据、理解变化、确认下阶段方案 |
| 我的 | 目标、限制、连接、数据与隐私设置 |

首页不做统计仪表盘。它只回答“今天发生了什么变化”和“我现在最适合做什么”。

### 5.2 Web：计划与回顾场

- Mission、训练计划和训练日历编辑；
- 证据板、周期对比与历史导出；
- 未来的教练协作与训练记录审核；
- 数据连接、隐私和账户管理。

## 6. 关键交互流程

### 6.1 首次进入或重返

```text
安全分流
  -> 选择当前状态与当下愿望
  -> 填写时间、器械和限制
  -> 生成首个低门槛行动或当前阶段 Mission
  -> 完成体验训练
  -> 形成第一条证据与下一步
```

### 6.2 日常训练

```text
10 秒状态检查
  -> 完整/短版/恢复版选择
  -> 执行与快速记录
  -> 主观难度/不适/完成原因
  -> 本次事实、待观察事项和下次建议
```

### 6.3 中断重返

```text
检测或主动声明中断
  -> 询问当前时间、状态与限制的最少信息
  -> 保留历史而不显示失败
  -> 提供重启、轻量恢复或重新设定 Mission
  -> 在完成后恢复正常复盘节奏
```

### 6.4 风险流程

```text
用户报告风险症状、急性疼痛或高风险状态
  -> 停止对应高强度建议
  -> 告知系统能力边界
  -> 提供安全的非诊断性下一步与专业支持建议
  -> 记录用户是否希望保留该限制
```

## 7. 决策引擎契约

v1 的计划引擎必须是规则优先、可审查的系统；生成式 AI 不能直接决定高风险训练内容。

每一个 `Decision` 必须包含：

- 输入事实及其来源；
- 被触发的规则或条件；
- 风险等级和不可越过的限制；
- 至少一个可替代的行动选项；
- 用户接受、修改或拒绝的结果；
- 下次复盘时需要验证的证据。

系统的推荐表达固定为：**观察到什么 -> 因此推断什么 -> 还不确定什么 -> 可以选择什么。**

## 8. 代表性用户路径

### 路径 A：中断重返

用户因工作忙停练 8 周。系统不展示断裂天数，而是用最少信息重新确认当前状态，提供 20 分钟低风险训练，并在两周内逐步恢复原训练节奏。

### 路径 B：建立规律

用户每周只能训练两次。系统以可完成的基础训练和替代方案建立重复节奏；当用户稳定完成后，再提出进入下一阶段计划的建议。

### 路径 C：自主进阶

用户已稳定训练，体重无明显变化，但表现、身体观察和主观感受出现不同信号。系统将这些证据放入同一阶段报告，指出当前无法判断的部分，给出维持、进阶或重新聚焦的候选路线，而不是只要求加训练或更严饮食。

## 9. v1 范围与非目标

### v1 必须验证

1. 用户能否理解当前 Mission 和今天的训练理由；
2. 用户能否在时间不足、恢复不足或中断后得到合适的下一步；
3. 用户能否从训练日志中看见可解释的进展，而非只看体重或热量；
4. 用户是否愿意确认或修改系统提出的阶段调整。

### v1 不验证

- 自动诊断动作质量、伤病或体重停滞原因；
- 全量营养管理或饮食治疗；
- 靠社交排名、打卡压力或虚构奖励驱动留存；
- 代替真人教练、医生、康复师或注册营养师。

## 10. 第一阶段执行清单

### 10.1 框架阶段交付物

1. 本文档经讨论确认后的 v1.0；
2. 三类用户状态的端到端旅程图；
3. App/Web 信息架构与低保真全局原型；
4. `CurrentState`、`Mission`、`Program`、`Session`、`Evidence`、`Review`、`Decision` 的字段和事件契约；
5. 首批规则决策表：安全分流、短版训练、恢复调整、中断重返、周复盘；
6. 8-12 位用户的原型测试脚本、记录表与结论。

### 10.2 进入开发前的通过标准

- 测试用户能准确复述：当前目标是什么、今天为什么这样练、系统依据什么调整；
- 三条用户路径均可从进入走到下一阶段，且没有依赖尚未实现的模块；
- 所有敏感数据和风险流程都有明确的权限与降级处理；
- 新增营养、动作识别、教练协作或 AI 功能时，不需要改写核心领域对象；
- 团队能以同一套术语讨论产品，避免“课程 App”“记录 App”“AI 教练”各自扩张。

## 11. 待验证假设

- 用户是否愿意把“阶段 Mission”作为训练主界面，而不是传统课程列表？
- 用户是否能理解事实、推断和建议的差异，并认为它比单一评分更可信？
- 用户是否愿意提供少量主观恢复与身体观察，以换取更好的解释？
- 对不同训练阶段而言，最有价值的“可解释下一步”分别是什么？
- 成长地图是否增强长期理解，而不被用户视为额外游戏负担？

这些假设在原型测试中验证后，才进入高保真设计、训练内容生产和实际开发。
